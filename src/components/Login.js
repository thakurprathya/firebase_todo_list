import React from 'react';
import '../styles/login.css';

const Login = () => {
    return (
        <div className='login'>
            <h1>Manage Todos</h1>
            <button>Sign in with Google</button>
        </div>
    );
}

export default Login;
